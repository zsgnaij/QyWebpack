import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

const testFun = [1, 2, 3].map(i => 1);

ReactDOM.render(<div>webpack</div>, document.getElementById('root'));